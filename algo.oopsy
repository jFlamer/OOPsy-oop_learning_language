method main() {
    var l: list = [10, 20, 30];
    var d: dict = {"a": 1};
    print(l[1]);
    print(d["a"]);

    var x: int = l[0];
    print(x);

    var numbers: list = [5, 8, 7];
    numbers[1] = 6;
    print(numbers[0]);
    print(numbers[1]);
    print(numbers[2]);

    for n in numbers do {
        print("Liczba: " + n);
    }
    return;
}